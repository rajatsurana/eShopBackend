{
  "name": "eSubziBackend",
  "version": "1.0.0",
  "description": "This repo features an express app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
  
}
